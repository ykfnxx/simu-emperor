[project]
name = "simu-emperor"
version = "0.1.0"
description = "回合制皇帝模拟游戏 - AI Agent 扮演官员"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "jinja2>=3.1",
    "aiosqlite>=0.20",
    "anthropic>=0.40",
    "openai>=1.50",
    "instructor>=1.5",
    "pyyaml>=6.0",
    "structlog>=24.0",
    "aiofiles>=23.0",
]

[project.scripts]
simu-emperor = "simu_emperor.player.web.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/simu_emperor"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "httpx>=0.27",
    "ruff>=0.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100
